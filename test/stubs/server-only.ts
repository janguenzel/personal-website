// Test stub for the `server-only` package. In a real RSC build this import
// throws if pulled into a client bundle; under Vitest we replace it with a
// harmless no-op so the surrounding pure logic can be unit-tested.
export {};
